# Automation Engine Landing Page

Static landing page for the workflow automation platform.

## Preview locally

From the repository root:

```bash
python -m http.server 8080 --directory landing
```

Open http://localhost:8080.

The page intentionally uses no build framework so it can be deployed to GitHub Pages, Netlify, Vercel static hosting, or any basic web server.
